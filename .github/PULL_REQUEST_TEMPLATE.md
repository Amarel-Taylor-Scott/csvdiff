## What does this PR do?

<!-- Matching/classification change? new CLI flag? output format? -->

## Checklist

- [ ] Tests added/updated and `pytest -q` passes (offline)
- [ ] `python -m compileall csvdiff` is clean
- [ ] Rows matched by key (reordering stays a no-op)
- [ ] Stays dependency-free (csv + argparse)
